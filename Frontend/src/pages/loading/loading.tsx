import "./loading.scss";

const Loading: React.FC = () => {

    return(
        <section className="page_loading">
            <h1>Loading...</h1>
        </section>
    )
};

export default Loading;